services:
  - type: web
    name: cubit-platform
    env: docker
    plan: free
    branch: main
    dockerComposeFilePath: ./docker-compose.yml
    buildFilter:
      paths:
        - ./**
    autoDeploy: true

